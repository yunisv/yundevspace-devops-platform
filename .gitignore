# Real secrets — never commit
.env
secrets/*
!secrets/.gitkeep

# OS
.DS_Store

# Rendered from access.yml.template by install.sh (contains the real subnets)
config/traefik/dynamic/access.yml

# Local backup archives (scripts/backup.sh) — contain secrets, copy them
# off-server instead of committing them anywhere
backups/
